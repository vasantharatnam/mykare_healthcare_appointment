# Backend Service

Spring Boot service for authentication, appointment management, database access, and Kafka event publishing.

## Running Locally

From the `backend/` directory:

```bash
mvn spring-boot:run